apiVersion: apps/v1
kind: Deployment
metadata:
  name: rating-service-deployment
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: rating-service
  replicas: 2
  template:
    metadata:
      labels:
        app.kubernetes.io/name: rating-service
    spec:
      containers:
        - name: rating-service-container
          image: localdevcluster-registry:5000/rating-service
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 250m
              memory: 200Mi
            limits:
              cpu: 400m
              memory: 400Mi
